Para iniciar o PuppetMaster usar o ficheiro "startup.cmd".
Dependendo do número de sites para uma dada configuração poderá ser necessário alterar a variável "numSlaves" no ficheiro "startup.cmd".
Antes de iniciar o script é necessário carregar uma configuração usando o botão "Load Configuration" e esperar que todos os slaves iniciem os processos do seu site. 
